services:
  homarr:
    container_name: homarr
    build:
      context: . # This points to the directory where your Dockerfile is located
    image: homarr:latest # Local image tag to use after building
    restart: unless-stopped
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ./homarr/appdata:/appdata
    environment:
      - SECRET_ENCRYPTION_KEY=a8397e220dda990d3354443d2ae7082c4c5d84457a7a663d22111db204c5213e
    ports:
      - "7575:7575"
